name: cdcine
description: O melhor streaming.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Rede e Imagem
  http: ^1.1.0
  cached_network_image: ^3.3.0
  # Player Poderoso
  flutter_inappwebview: ^6.0.0
  # Visual Profissional
  google_fonts: ^6.1.0
  carousel_slider: ^4.2.1   # Para o Carrossel
  shimmer: ^3.0.0           # Efeito de carregamento (esqueleto)
  # Dados Locais (Histórico)
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
